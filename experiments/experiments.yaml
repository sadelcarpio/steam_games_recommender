tracking_uri: http://localhost:5000
default_env_manager: local
exp_name: steam-recsys

experiments:
  - name: pop-model-baseline
    project_uri: ./pop_model_baseline
    entry_point: main
    parameters:
      duckdb_path: ../data/steam.duckdb  # relative to experiments dir. Resolves in mlflow.run
    tags:
      model_family: popularity

  - name: tf-idf-content-based
    project_uri: ./tf_idf_content_based