[project]
name = "baseline-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "libs>=0.1.0",
    "pandas>=2.2.3",
]

[tool.uv.sources]
libs = { path = "../libs", editable = true }
